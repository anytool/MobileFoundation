var workspace   = require("ANYWorkspace");
var __rN        = require("ReactNative");
/* a:instance, b:callbacks */
module.exports  = function(a, b) {
    // get id
    var id = a.__props.rActId || "";
    // node exists
    if (workspace.aliases[id]) {
        console.log("[debug] reload action: ", workspace.aliases[id], a.__props.rProps);
    }
};